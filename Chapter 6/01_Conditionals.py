# In python programming too, we must be able to execute instructions on a condition(s) being met.
# # This is what conditionals are for!

# If Else Conditionals:
a = int(input("Enter the age"))

if(a>18):
    print("You are an Adult") 
    print("Good for you")    # If you are inside if else loop you 
    # have to be in the indented block i.e. the space it gives after 
    # if or else that space is called indented block, so to be inside
    # if else you have to be in the indented loop and to get out of 
    # if else loop you have to come out of that space it generates 
    # and go fully left side that is where no space no indented 
    # block is made 
else:
    print("Not an Adult")
    
print("End of program")
